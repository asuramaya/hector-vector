# Core runtime — the server itself only needs these plus the Python stdlib.
Pillow>=10.0
numpy>=1.24

# Optional, installed on demand from the Settings UI into ./.venv (not required to start):
#   rembg[cpu] onnxruntime   # AI background removal (Cutout "AI" backend)
#   cairosvg                 # rasterize curved (vtracer) SVGs in Export PNG; needs system libcairo
#
# External tools auto-install on first run (no pip needed):
#   Real-ESRGAN NCNN Vulkan  -> downloaded from GitHub releases into ./tools
#   VTracer                  -> built via `cargo install vtracer` into ./tools/cargo
